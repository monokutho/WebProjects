var albumCover = document.getElementById("albumCover");
var portHeight = window.innerHeight;

function resizeCover() {
    
    var h = Math.floor(portHeight/8) + "px";
    /*albumCover.setAttribute("height", "60px");
    albumCover.setAttribute("width", "60px");
    albumCover.setAttribute("background-color", "white");
    console.log(albumCover);
    */
    
    
    
}

function init() {
    resizeCover();
}